# Cloudflare Workers (Static Assets) — deploy via `npx wrangler deploy`
# ⚠️ NÃO é git-connected: push NÃO publica. Worker: lp-vibe-render-upsell
# Domínio: viberenderup.montani3d.com.br (custom domain do worker)
name = "lp-vibe-render-upsell"
compatibility_date = "2026-06-25"

[assets]
directory = "."
